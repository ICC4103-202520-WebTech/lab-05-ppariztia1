<h1>Reservations</h1>

<ul>
    <% @reservations.each do |reservation|%>
        <li>
            <%= link_to "#{reservation.reservation_status}", reservation_path(reservation.id) %>
        </li>
    <% end %>
</ul>

<%= link_to "Back to Home", home_path %>